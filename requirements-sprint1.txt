# Dependencias mínimas para correr el Sprint 1 (flujo Unsecure).
#
# A propósito NO fijamos versiones exactas aquí (a diferencia de
# requirements.txt): las versiones exactas que se probaron originalmente
# fueron publicadas antes de que existiera Python 3.14, así que su
# dependencia pydantic-core (que es una extensión nativa en Rust) no tiene
# binario precompilado para 3.14 y falla al compilar desde código fuente.
# Dejando el rango abierto, pip instala automáticamente una versión más
# reciente que sí trae el binario listo.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.10
pydantic-settings>=2.6
python-dotenv>=1.0
anthropic>=0.34
httpx>=0.27
pytest>=8.3
pytest-asyncio>=0.24
